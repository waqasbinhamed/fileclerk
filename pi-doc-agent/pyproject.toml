[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [".venv", "dist", "build"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line too long — black handles this
]

[tool.black]
line-length = 100
target-version = ["py311"]
